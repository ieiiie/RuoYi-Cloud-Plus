# 应用品牌与友好加载失败提示

## 本次调整

- bell-plus 组合应用左上角使用授权应用描述中的名称和图标，与工作台应用卡片一致；空图标回退为通用应用图标。
- `BasicLayout` 透传 `logoIcon`，`VbenLogo` 优先显示应用图标，并保留名称悬停提示及侧栏折叠行为。
- 无应用图标的 SaaS 主布局仍显示当前租户品牌，未修改 DBO、应用配置或租户数据。
- 组合应用及独立应用资源加载失败时，共用 `application-load-error.vue`，显示应用名称、稍后重试和联系管理员的说明，不渲染内部 URL 或原始错误。
- 组合应用重试沿用原有整页刷新策略，保留当前深链接；独立应用沿用现有沙箱重新加载策略。返回工作台使用 `/dashboard`。

## 验证

- 前端类型检查、定向 ESLint、生产构建通过。
- 9 项定向测试通过：应用图标与折叠/租户回退 3 项，错误提示与操作 2 项，原租户品牌 4 项。
- 错误提示单元测试隔离了 UI 库 Button，避免测试环境被无关日期组件的 ESM 导入错误阻断；实际 Button 行为另经浏览器验证。
- 浏览器访问 `/test/agriculture/workspace`，确认左上角应用图标及 test 名称、新错误提示、重试后保留原地址。
- 点击返回工作台后到达 `/dashboard`，名称恢复默认管理租户。
- 农业子应用仍处于加载失败状态。本次完成的是应用品牌和错误体验调整，不代表其服务已恢复。
- 未提交、推送代码，未修改后端或运行配置。
